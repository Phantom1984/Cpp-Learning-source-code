#include"vector.h"
/*
int main()
{
	vector<int>v1;
	for (int i = 0; i < 10; i++)
		v1.push_back(i);

	vector<int>v2;
	for (int i = 10; i > 0; i--)
		v2.push_back(i);

	cout << "交换前：" << endl;
	cout << "v1="; printVector(v1);
	cout << "v2="; printVector(v2);

	v1.swap(v2);

	cout << "交换后：" << endl;
	cout << "v1="; printVector(v1);
	cout << "v2="; printVector(v2);

	//实际用途：巧用swap可以收缩内存空间

}*/